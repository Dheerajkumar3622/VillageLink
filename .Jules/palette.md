## 2024-05-22 - Auth Form Accessibility
**Learning:** High-traffic forms like Login/Register often miss basic `htmlFor`/`id` associations or use placeholders as labels, which fails accessibility standards.
**Action:** When auditing forms, prioritize connecting existing visual labels with `htmlFor` and adding `aria-label` to inputs that rely solely on placeholders.
